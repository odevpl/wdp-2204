/* selectors */
export const getAllPromotion = state => state.promotion;

/* actions creators */
const promotionReducer = (statePart = [], action) => {
  switch (action.type) {
    default:
      return statePart;
  }
};

export default promotionReducer;
