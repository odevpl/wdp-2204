/* selectors */
export const getAllFeedback = ({ feedback }) => feedback;

/* reducer */
export default function reducer(statePart = [], action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
