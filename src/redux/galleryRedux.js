/* selectors */
export const getAllGalleryItem = state => state.gallery;

/* actions creators */
const galleryReducer = (statePart = [], action) => {
  switch (action.type) {
    default:
      return statePart;
  }
};

export default galleryReducer;
